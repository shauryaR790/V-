# Formatting

```powershell
vpp fmt path/to/file.vpp
```

Re-indents blocks and normalizes spacing. Does not change semantics.

Format before committing. IDE format-on-save can call `vpp fmt` via tasks (manual setup today).
