# std.string

String operations via the stdlib module. See [`std/string.vpp`](../../std/string.vpp) for current exports.

```vpp
import std.string
```

Built-in `string` type supports concatenation with `+` in the language.
