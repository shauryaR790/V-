name: Deploy website

on:
  push:
    branches: [main]
    paths:
      - "website/**"
      - "course.pdf"
      - "vpp_20_course_deep_curriculum.pdf"
      - "docs/**"
      - "projects/**"
      - "SPEC.md"
      - "ARCHITECTURE.md"
      - "MEMORY_MODEL.md"
      - "CHANGELOG.md"
      - "CONTRIBUTING.md"
      - "README.md"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Generate documentation pages
        run: python website/generate_pages.py
      - uses: actions/upload-pages-artifact@v3
        with:
          path: website
      - id: deployment
        uses: actions/deploy-pages@v4
